# 컨텍스트 노트 — 작업 결정 기록

작업 중 내린 결정과 그 이유를 기록합니다.
새로운 결정이 생길 때마다 이 파일에 추가하세요.

---

## 2025 — 초기 설계 결정

### 배포 플랫폼: Netlify → Vercel로 변경

Netlify 무료 플랜의 크레딧 기반 과금 구조가 이 프로젝트의 사용 패턴과 맞지 않았습니다.
5초 polling 기준 월 392 크레딧이 필요한데 무료 플랜은 300 크레딧으로 초과합니다.
Vercel은 리소스 기반 과금이라 월 4회, 6시간, 20명 규모에서 무료 플랜으로 충분히 운영 가능합니다.

### 프레임워크: Vite React → Next.js 16 App Router로 변경

Vercel 선택 후 API Routes(Route Handlers)를 자연스럽게 쓰려면 Next.js가 가장 적합합니다.
별도 서버리스 함수 설정 없이 `src/app/api/` 경로만으로 엔드포인트를 구성할 수 있습니다.

### GAS 완전 제거 결정

초기에는 GAS를 API 서버로 유지하는 안을 검토했으나,
GAS의 `google.script.run` 방식은 외부 HTTP 호출과 호환되지 않아 `doGet`/`doPost` 전면 재작성이 필요했습니다.
재작성 공수가 크고 장기적으로 기술부채가 되므로 Vercel Route Handlers로 완전 대체하기로 결정했습니다.

### 실시간 동기화: Polling 30초 간격 채택

Google Sheets는 WebSocket을 지원하지 않습니다.
5초 polling은 Vercel 무료 플랜에서도 사용 가능하지만, 출석부 특성상 초단위 동기화가 불필요하고
30초 polling + Optimistic Update 조합으로 사용자 경험과 서버 비용 모두 만족할 수 있습니다.

Polling이 필요한 근본 원인은 **호스팅(Vercel)이 아니라 데이터 소스(Google Sheets)** 입니다.
Google Sheets API는 셀 변경을 push/webhook으로 알려주는 기능이 없고, Vercel의 서버리스 함수도
WebSocket 같은 영속 연결 유지에 적합하지 않습니다. 따라서 호스팅을 바꿔도 polling 자체는 사라지지 않습니다.
진짜 실시간 push가 필요하면 Supabase Realtime/Firebase 같은 DB로 교체해야 하지만,
그러면 "비개발자 교역자가 시트를 직접 열어 수정 가능"하다는 이 프로젝트의 핵심 장점을 잃게 되므로 채택하지 않습니다.

### 출석 상태 저장 방식: 레코드 유무로 판단

GAS 원본 코드 분석 결과, Attendance 시트에 `"출석"` 레코드가 존재하면 출석,
없으면 결석으로 간주하는 구조입니다. 별도 `"결석"` 값은 저장하지 않습니다.
마이그레이션 후에도 이 규칙을 동일하게 유지합니다 (기존 데이터 호환성).

### 관리자 인증: 세션 기반 간이 인증

별도 OAuth나 JWT 도입 없이 비밀번호 모달 → `POST /api/auth` 검증 → `sessionStorage` 토큰 저장 방식을 채택합니다.
공개 접근 서비스이고 관리자가 소수(교역자 수준)이므로 이 정도 보안 수준이 적합합니다.
비밀번호는 Vercel 환경변수(`ADMIN_PASSWORD`)에 보관합니다.

---

## Phase 1~2 — 프로젝트 세팅 및 출석체크 메인 UI 구현

### 마이그레이션 순서 변경: API 연동보다 UI 구현을 먼저 진행

원래 계획은 "Phase 2: API 연동 → Phase 3: 화면 구현"이었으나, 타입(`src/types/index.ts`)이 이미 확정되어 있어
목업 데이터로 먼저 화면을 만들고 나중에 실제 API로 교체하는 편이 더 효율적이라고 판단했습니다.
화면이 먼저 보여야 UX 문제를 일찍 발견할 수 있고, API 연동 후 수정 범위도 줄어듭니다.
이에 따라 진행 순서를 "UI 구현(목업) → API 연동 → UI-API 연결 → 배포"로 재조정했습니다.

### `create-next-app`은 빈 디렉토리가 아니면 실행 거부

`CLAUDE.md`, `docs/` 등 기존 파일이 있는 상태에서 `create-next-app .`을 실행하면 충돌로 거부됩니다.
임시 폴더(`tmpapp`)에 생성한 뒤 `src/`, `public/`, 설정 파일들만 골라 이동하고 임시 폴더를 삭제하는 방식으로 해결했습니다.
이 과정에서 `create-next-app`이 생성한 기본 `.gitignore`가 기존에 작성해둔 보안 강화 버전
(서비스 계정 키 패턴 `*-service-account.json`, `.vscode/*` 규칙 포함)을 덮어썼던 것을 발견해 원본으로 복원했습니다.

### 목업 데이터에는 절대 실제 캡쳐 속 이름을 쓰지 않음

UI 디자인 참고용으로 받은 스크린샷에는 실제 학생/교사 이름이 포함되어 있었습니다.
`docs/security-checklist.md` 정책에 따라 레이아웃/구조만 참고하고, 코드에 들어가는 목업 데이터는
스크린샷에 없는 더미 이름(홍길동, 김철수 등)으로 새로 작성했습니다 (`src/lib/mock-data.ts`).

### Next.js 16.2.9의 `next/font/google`에 한국어 서브셋 메타데이터 누락

`Hahmlet`, `Gowun_Batang`, `IBM_Plex_Sans_KR` 등 한글 폰트에 `subsets: ["korean"]`을 지정하면
"`korean`이 아니라 `latin`/`latin-ext`/`vietnamese`만 허용된다"는 타입 에러가 발생합니다.
이 Next.js 버전에 번들된 `next/font/google` 타입 정의 자체에 `korean` 서브셋이 어떤 폰트에도 존재하지 않는
버그성 누락으로 확인했습니다 (`node_modules/next/dist/compiled/@next/font/dist/google/index.d.ts` 확인).
해결: `subsets` 옵션을 생략하면 정상적으로 한글이 렌더링됩니다. 추후 Next.js 버전을 올리면 이 제약이 풀렸는지 재확인이 필요합니다.

### 출석체크 메인 UI 비주얼 컨셉: "주일 출석부" 티켓/스탬프 디자인

`frontend-design` 스킬로 메인 페이지를 리디자인하면서 출석 카드를 티켓 스텁(펀치홀, 도장 애니메이션) 모티프로 잡았습니다.
이후 사용자 피드백으로 색상을 빈티지한 크림톤에서 고등학생 타겟에 맞는 밝고 채도 높은 팔레트로 교체했고,
"산뜻함"과 상충되는 종이 그레인 텍스처는 제거했습니다. 티켓 펀치홀/스탬프 애니메이션 등 구조는 유지했습니다.
참고 캡쳐에 "출석 처리된 상태"의 색상 예시가 없어서, 학년/교사/새친구 구분 없이 출석 시에는 모두 동일한
스탬프 색(테라코타)으로 통일하기로 직접 결정했습니다 (그룹 구분은 평상시 테두리 색으로만 표시).

### 헤더 액션 버튼: 사용 빈도 기준으로 시각적 위계 재조정

초기 구현에서는 관리자 전용 저빈도 기능(`학생 관리`)이 가장 진하고 우측 끝(주 CTA 자리)에 있었고,
매주 쓰는 `출석현황`은 가장 옅은 버튼이라 실제 사용 빈도와 시각적 강조가 어긋났습니다.
`출석현황`을 채워진 배경으로 격상하고, `학생 관리`는 자물쇠 아이콘 + 구분선으로 "보기" 그룹과 분리했습니다.
`새로고침` 버튼은 30초 polling이 이미 자동 갱신하므로 제거하고 "마지막 업데이트 HH:MM" 텍스트로 대체했습니다
(날짜 표시 옆에 배치 — 액션 버튼과 성격이 달라 같은 줄에 섞으면 모바일에서 줄바꿈 시 정렬이 흐트러짐).

### 헤더 반응형: `flex-wrap` 즉흥 줄바꿈 대신 `lg` 기준 명시적 2단 레이아웃

화면 폭이 애매한 중간 구간(~800px)에서 `flex-wrap`이 컨텐츠 폭에 따라 즉흥적으로 줄을 바꾸면서
좌측 정렬된 콘텐츠 우측에 의도하지 않은 빈 공간이 생기는 문제가 있었습니다.
`lg`(1024px) 미만에서는 항상 날짜/세션 줄과 액션 버튼 줄을 각각 `w-full justify-between`으로 강제하고,
`lg` 이상에서만 한 줄로 합치도록 명시적으로 분기해 애매한 구간을 없앴습니다.

### 클라이언트 전용 타임스탬프는 `useState` 초기값이 아니라 `useEffect`에서 설정

"마지막 업데이트" 텍스트를 `useState(() => new Date()...)`로 초기화하면 서버 렌더링 시각과
클라이언트 하이드레이션 시각이 분 단위로 갈릴 때 React 하이드레이션 불일치가 발생할 수 있습니다.
`useEffect`로 마운트 후에만 값을 설정하도록 분리해 이 문제를 원천적으로 방지했습니다.

---

## Phase 3 — Google Sheets API 연동

### Students 시트의 `출석률` 컬럼은 헤더만 있고 데이터는 비어있음

실제 운영 스프레드시트 확인 결과 Students 시트에 `Notes` 뒤로 `출석률`/`세례`/`gender` 3개 컬럼이 있었습니다.
`세례`와 `gender`는 실제 값이 채워져 있지만, `출석률`은 헤더만 있고 모든 행이 빈 값입니다.
레거시 GAS 코드(`docs/legacy-gas.json`)에도 `출석률`을 Students 시트에 저장하는 로직은 없고
`getStudentAttendanceStats` 같은 함수로 Attendance 기록을 매번 스캔해 계산하던 것과 정황이 일치합니다 —
즉 이 컬럼은 실제로 쓰인 적 없는 placeholder로 추정됩니다.
타입(`src/types/index.ts`)에는 `attendanceRate`/`baptism`/`gender`를 모두 추가했지만,
Phase 3/4에서 `attendanceRate`는 항상 빈 문자열로 읽힐 것을 전제로 하고 별도 계산 로직을 만들지 않습니다.
이후 출석률 표시/계산이 실제로 필요해지면 레거시처럼 Attendance 기록 기반 계산 함수를 새로 만들어야 합니다.

### Attendance 시트의 `Ect` 컬럼은 `Type`의 오타가 아니라 별개의 비고 컬럼

처음에는 실제 운영 시트 I열 헤더가 `Type`의 오타로 `ect`가 된 것으로 보고 그렇게 기록했었으나,
`Ect` 컬럼의 실제 데이터 값(`타교회 선교(OO목사님 확인)` 등 출석 예외 사유 자유 텍스트)을 확인한 결과
`student`/`teacher` 구분과는 전혀 무관한 **별개의 기타 비고 컬럼**임이 드러나 정정합니다.
레거시 GAS 스크립트의 헤더 생성 코드는 분명히 9번째 컬럼을 `'Type'`으로 만들었지만(`appendRow([...,'Timestamp','Type'])`),
실제로 그 컬럼에 student/teacher 값을 쓰는 코드는 어디에도 없었습니다 — 즉 운영 중 누군가
그 자리에 `Type`이 아니라 `Ect`라는 별도 비고 컬럼을 만들어 쓰기 시작한 것으로 보입니다.

**결정**: `Ect` 컬럼은 그대로 유지하고 `AttendanceRecord.note`로 매핑합니다.
교사 출석 구분용 `Type`은 운영 시트에 존재하지 않으므로 Phase 3에서 **10번째 컬럼으로 신규 추가**해서
`AttendanceRecord.type`에 매핑합니다. `appendRow`로 행을 쓸 때는 9번째(`Ect`) 자리를 빈 문자열로 두고
10번째에 `type` 값을 씁니다 — `[date, session, grade, class, studentId, name, '출석', timestamp, '', type]`.

---

## Phase 7 사전 검증 — 검증용 사본 시트 준비 절차 (사본 교체 시 재실행)

실시트가 업데이트되어 검증용 사본을 새로 만들 때마다, 새 사본에는 검증에 필요한
변경 2가지를 다시 적용해야 합니다. 두 작업 모두 **멱등 스크립트**로 만들어져 있어
아래 순서대로 실행만 하면 됩니다 (이미 적용된 사본에 재실행해도 안전).

**사본 교체 절차:**

1. (수동) 실시트 → 사본 만들기 → 사본에 서비스 계정 이메일을 **편집자**로 공유
   - 사본이 공유 폴더에 생성되면 다른 사람 권한이 상속되므로 개인 위치로 이동 후 불필요한 권한 제거
2. (수동) `.env.local`의 `GOOGLE_SPREADSHEET_ID`를 새 사본 ID로 교체
3. `node scripts/verify-sheets.mjs` — 탭/헤더 구조가 코드 기대값과 일치하는지 확인
4. `node scripts/verify-data.mjs` — **Attendance에 `Type` 헤더 자동 추가**(없을 때만) + 데이터 형식 점검
5. `node scripts/migrate-newfamilies.mjs` — **NewFamilies 탭 → Students `Grade='새친구'` 마이그레이션**
   (ID 기준 중복 건너뜀, NewFamilies 탭은 보존, ID 없는 행은 제외 후 보고)
6. (선택) dev 서버 실행 후 `node scripts/verify-read-api.mjs` / `node scripts/verify-write-api.mjs`로 재검증

**참고 사항:**

- 검증 중 발견된 코드 버그 2건(stats 라우트의 레거시 행 교사 판별, 출석 현황 차트 라벨 밀림)은
  코드 쪽 수정이라 사본 교체와 무관하게 유지됩니다.
- 실시트에서 수동 수정이 권장되는 데이터 오류(Birthdate 오타, ID/Class 빈 값 등)는
  사본 교체 시 행 번호가 달라질 수 있으나, 4번 `verify-data.mjs`가 다시 검출해 줍니다.
  실시트에서 먼저 고치면 새 사본에는 문제가 아예 없습니다.
- Phase 7 실시트 전환 시에도 같은 절차의 4~5번(Type 헤더 + 새친구 마이그레이션)을
  실시트에 그대로 적용하면 됩니다 (Type 헤더는 Step 6 GAS 호환성 결론 확인 후).

---

## Phase 7 사전 검증 — 결과 및 결정 기록

실시트 사본으로 전 API·전 화면을 검증 완료했습니다. 세부 체크리스트와 수치는 CLAUDE.md 진행 상태 참조.
여기에는 결정 사항과 그 이유만 기록합니다.

### 새친구 데이터 모델: NewFamilies 탭 → Students `Grade='새친구'`로 통합

실제 운영에서 새친구는 Students가 아닌 `NewFamilies` 탭(19컬럼 별도 스키마)에 등록되고,
등반 시 Students로 이동(ID 유지)하는 구조였습니다. 새 앱은 처음부터 Students의 `Grade='새친구'` 행으로
설계했으므로, **NewFamilies를 Students로 마이그레이션하는 방식**을 채택했습니다 (`scripts/migrate-newfamilies.mjs`, 멱등).
잔여 필드(본래학년/등록일/인도자 등)는 Notes에 합칩니다. NewFamilies 탭은 레거시 GAS 병행 운영을 위해 보존합니다.
병행 기간 중 새로 등록되는 새친구는 한쪽에만 보이므로, 전환 직전에 스크립트를 재실행해 동기화합니다.

### stats 라우트의 교사 판별: Type 컬럼 + Grade='선생님' fallback

레거시 출석 행은 `Type`이 빈 값이고 `Grade='선생님'`으로 교사를 구분합니다(1,533건).
초기 코드는 `Type===''`을 학생으로 집계해 교사 통계가 0%로 나오는 버그가 있어,
`isTeacherRow`(Type='teacher' 또는 Grade='선생님') 판별로 수정했습니다.
전체 출석 합산도 재적 학년(gradeCount에 존재하는 학년)만 포함해, 명단에 없는 레거시 '새가족' 기록이
분모 없이 분자만 부풀리는 것을 방지했습니다.

### ID 생성 형식: 새 앱 코드 형식 유지 확정

실데이터 학생 ID 348건은 `YY-학년-반-순번(2자리)` 형식이지만, 최신 GAS의 `generateStudentId`는
새 앱과 동일한 `YYYY-학년-반-순번(3자리)`을 생성합니다 — 구형식은 과거 데이터일 뿐입니다.
연도 프리픽스가 달라 충돌이 불가능하고, 양쪽 어디에도 ID를 파싱하는 코드가 없어 형식 혼재는 무해합니다.
교사 ID도 같은 이유로 새 앱 형식(`T-YYYY-NNN`)을 유지합니다.

### 실시트 Type 컬럼 추가: GAS 호환 확인 완료

GAS `markAttendance`는 앞 8개 컬럼만 `appendRow`하고, 삭제/읽기는 전부 헤더 이름 기반이므로
Attendance 맨 끝(J열)에 `Type`을 추가해도 GAS는 영향받지 않습니다. GAS가 계속 쓰는 행은 Type이
비지만 위의 fallback으로 처리됩니다. (GAS는 6/3 내보내기 이후 수정 없음 — 사용자 확인)

### recharts 스택 막대의 LabelList는 index 대신 값으로 행을 찾아야 함

값이 0인 스택 세그먼트는 라벨 목록에서 빠져 `index`가 데이터 배열과 어긋납니다
(출석 현황에서 결석 0명인 팀이 있으면 이후 행 라벨이 한 칸씩 밀리던 버그의 원인).
`dataKey`를 카테고리명으로 주고 `value`로 행을 역조회하는 방식으로 수정했습니다 (`GroupAttendanceChart.tsx`).

### 반 정렬은 numeric localeCompare 필수

반 번호가 문자열이라 기본 localeCompare로는 10반이 1반과 2반 사이에 옵니다.
`localeCompare(..., { numeric: true })`로 수정 (`group-members.ts`, `birthdays.ts`).
테스트 시트에는 10반 이상이 없어 실데이터 검증에서만 드러난 문제였습니다.

### 실시트에서 수동 수정 필요한 데이터 (전환 전 권장)

행 번호는 검증 당시 사본 기준이며, `scripts/verify-data.mjs`가 재검출 가능:
Students 81행(Birthdate 하이픈 오타), 115행(ID 없음), 272행(Class 없음), NewFamilies 11행(ID 없음).
115·272행은 레거시 화면에서 아예 숨겨져 출석 체크가 불가능했던 학생들로, 새 앱 인원수(267)와
레거시(265)의 차이 원인이기도 합니다. 수정 시 양쪽 모두 267로 일치합니다.

### 데이터 불완전 인원 처리: 레거시처럼 숨기지 않고 "확인 필요" 그룹으로 노출

실운영 중 학생 정보를 구글 시트에서 직접 수정하다가 필수 필드(ID·학년·반·이름, 교사는
ID·팀·이름)가 비는 경우가 실제로 발생함을 확인 (사본에서 "정서희" 반(Class) 값이 세션
이동 중 비워진 사례 발견 → 검증 스크립트로 "최승민" ID 누락 사례도 추가 확인).

레거시 GAS의 `getStudents`/`getTeacherMembers`는 이런 행을 `continue`로 조용히 숨기는데,
처음엔 새 앱도 그렇게 맞출지 검토했으나 **반대로 결정**했습니다. 이유:
- 숨기면 그 사람은 출석 체크 자체가 불가능해지고 통계에서 "쭉 결석"으로 잘못 집계됨
- 조용히 사라지므로 데이터가 잘못된 것 자체를 아무도 눈치채지 못한 채 방치됨
  (실제로 레거시 265명 vs 새 앱 267명 차이가 이렇게 오래 발견되지 않았던 사례)
- 반면 화면에 이상하게라도 보이면("2-반"처럼 라벨이 깨진 형태) 발견·수정을 유도하는
  효과가 있음 — 실제로 이번에 이 방식으로 정서희 사례를 발견함

**결정**: 숨기지 않되, `2-반` 같은 애매한 라벨 대신 학년/교사 구분 없이 하나로 모은
"확인 필요" 그룹(`group-members.ts`의 `variant: "incomplete"`)으로 명시적으로 노출.
각 카드에 어떤 필드가 비었는지(`반 없음`, `ID 없음` 등) 바로 보이도록 표시해 조치를
바로 취할 수 있게 함. 새친구는 반 개념이 없는 게 정상이라 반 누락 판정에서 제외.
`/`, `/history`, `/members` 세 화면이 전부 같은 `groupStudentsAndTeachers`를 쓰므로
한 번의 수정으로 전체 반영됨. ID가 없는 인원은 화면 표시용 임시 키만 부여하고, 실제
출석 토글 대상 식별은 시트에 ID가 채워지기 전까지 여전히 불가능함(별도 미해결 사항).

---

## 교사 음력 생일 지원

### 배경 및 결정

`TeacherForm`(교사 정보 수정 팝업)에만 "음력" 체크박스를 추가해, 체크된 교사의 저장된
Birthdate를 음력 날짜로 간주하도록 함. 학생(`StudentForm`)에는 적용하지 않음(요청 범위 밖).

- **저장 방식**: Teachers 시트에 `Lunar`(`TRUE`/`FALSE`) 컬럼을 맨 끝에 추가.
  Attendance의 `Type` 컬럼 추가와 동일한 패턴(GAS는 헤더 이름 기반이라 끝에 컬럼을 더해도 무해).
  `scripts/add-teacher-lunar-header.mjs`로 추가(멱등) — **Phase 8-B 실시트 전환 시 재실행 필요**.
- **음력→양력 변환**: `korean-lunar-calendar`(KASI 기준, zero-dep) 신규 의존성 추가.
  `src/lib/lunar.ts`의 `lunarToSolar(year, month, day)`가 해당 연도의 양력 월/일을 반환.
  윤달(간지 윤월) 입력은 지원하지 않고 항상 평달로 간주 — 체크박스 하나만 두기로 했고,
  실제로 극히 드문 케이스라 UI를 늘리지 않기로 결정. 변환 실패(지원범위 1000~2050 밖 등)
  시 원본 월/일로 폴백하고 "(음력)" 표시도 생략(조용히 양력처럼 취급).
- **표시**: `/birthday`에서 음력 생일자 카드에 `MM/DD (음력)`로 표시(`isLunar` 플래그).
  변환은 "저장된 음력 월/일을 조회 시점의 연도로 환산"이므로 해마다 자동으로 날짜가 바뀜.
- **`groupBirthdaysByMonth` 시그니처 변경**: `year` 파라미터 추가 필수 — 호출부(`birthday/page.tsx`)에서
  `getTodayInSeoul().getFullYear()`를 넘겨줌.

---

## Phase 8-B — 실시트 전환 (2026-07-17)

### 전환 타임라인과 "되돌릴 곳이 없음"

레거시 GAS는 2026-07-12 예배를 끝으로 은퇴했고, 2026-07-17에 실시트 전환을 마쳐
2026-07-19 주일부터 새 앱이 단독 운영됩니다.

계획 단계에서는 "GAS 병행 운영 = 롤백 경로"를 전제했으나, 실제로는 전환 시점에 이미
GAS가 내려간 상태였습니다. 테스트 사본은 6월 말 이후 데이터가 없어 운영 대체재가 못 되므로
**장애 시 대안은 구글 시트 직접 입력뿐**입니다. 사본을 "롤백용"으로 표현한 계획 문구는
정확하지 않았고, 사본의 실제 가치는 아래의 개발용 안전지대입니다.

### 새친구 중복 사고 — ID만으로 동일인을 판정하면 안 된다

**이 전환에서 가장 값비싼 교훈입니다.** `migrate-newfamilies.mjs`가 NewFamilies 행을
Students로 옮길 때 **ID 일치**만으로 중복을 걸렀는데, 이 판정이 놓치는 경우가 실제로 있었습니다.

새친구로 방문했다가 정식 반으로 **등반하면서 새 학생 ID를 발급**받은 사람은, 옛 새가족 ID와
새 학생 ID가 달라 "처음 보는 사람"으로 판정됩니다. 정상 처리된 6명은 등반 시 NewFamilies에서
행이 제거됐지만(=`등반한 새친구` 탭 ID 6건 중 NewFamilies 잔존 0건), 한 명만 수작업 누락으로
행이 남아 있었고 그 결과 **같은 사람이 정식 학생과 새친구로 이중 등록**됐습니다
(81행 3학년 2반 출석 28건 ↔ 363행 새친구 출석 1건, 이름·생년월일·성별·학교·연락처 전부 동일).

레거시 대조에서 오전 인원이 예상 267이 아닌 268로 나와 발견했습니다. **숫자가 안 맞는다는
신호가 없었으면 조용히 지나갔을 문제**입니다.

- **판정 키를 `이름+생년월일`로 확장**. 단 **이름만으로는 판정하지 않음** — 실데이터에
  동명이인이 여러 쌍(3명이 같은 이름인 경우 포함) 존재하므로, 이름만 비교하면 서로 다른
  사람을 같은 사람으로 보고 **진짜 새친구를 누락**시킵니다. 중복은 눈에 띄지만 누락은
  조용하므로 더 위험합니다. 따라서 생년월일이 비면 판정을 포기하고 이전합니다.
- 건너뛴 행은 NewFamilies **행 번호와 함께 출력**해 수동 정리를 유도합니다.
- **정리 방법**: Students 행은 앱의 삭제 버튼이 아니라 **시트에서 직접 삭제**해야 합니다.
  앱 삭제는 cascade delete라 그 사람의 출석 기록까지 지웁니다. 위 사례의 3/1 출석 1건은
  실제로 있었던 사실이므로 고아 레코드로 보존하는 편이 맞습니다.
- ⚠️ 이 동일인 판정 분기는 **실데이터로 검증되지 않은 휴면 코드**입니다. 사고 행을 이미
  삭제한 뒤 수정해 재현 조건이 사라졌고, GAS 은퇴로 상시 실행 대상도 아니게 됐습니다.
  **연도별 시트 갱신(`docs/yearly-sheet-operation.md`) 때 이 코드가 처음 실행됩니다.**

### 환경 분리보다 단순함: Preview·`.env.local` 모두 실시트로 통일

처음에는 "Preview와 로컬은 사본, Production만 실시트"로 분리해 실데이터를 보호하려 했으나
**둘 다 실시트로 통일**하기로 했습니다.

- **Preview**: 실제로 열어보는 워크플로가 없음(로컬 확인 후 바로 Production 배포). 방문하지
  않는 Preview 배포는 시트를 읽지도 쓰지도 않아 방어 대상이 없습니다. 반면 사본은 전환 후
  아무도 쓰지 않아 계속 낡으므로, 어쩌다 열었을 때 **낡은 데이터로 오판할 위험이 더 큽니다.**
- **`.env.local`**: 1인 개발이라 본인이 무엇을 조작했는지 알고 있고 출석 토글은 행 추가/삭제라
  되돌리기 쉽습니다. 낡은 데이터를 보는 손해가 더 큽니다.
- ⚠️ **예외는 삭제 플로우**: `/members`의 학생/교사 삭제는 cascade delete라 앱에서 되돌릴 수
  없고 1년치 출석이 소실됩니다. 삭제 기능을 시험할 때는 그때만 `.env.local`을 사본으로 바꿉니다.
  **테스트 사본을 남겨두는 이유가 이것**(롤백용이 아니라 개발용 안전지대)입니다.
- 사고 시 최종 복구 수단은 구글 시트의 **파일 > 버전 기록**입니다.

### 검증 스크립트의 기대값은 조용히 낡는다

전환 준비 중 검증 스크립트 두 개가 모두 낡아 있었습니다. **통과했지만 정작 이번에 바꾼 것을
검증하지 않는 상태**였다는 게 공통점입니다.

- `verify-sheets.mjs`: Teachers 기대 헤더에 `Lunar` 없음 + `KNOWN_MISSING`이 Attendance
  `Type`을 "없는 게 정상"으로 취급(Phase 7 잔재) → 누락을 전부 차단(❌)으로 바꾸고, 조치
  방법(`REMEDY`)을 함께 출력해 **사전 점검 결과가 곧 할 일 목록**이 되도록 함
- `verify-read-api.mjs`: 교사 기대 키가 8개라 `lunarBirthdate`를 검사하지 않음 → 키 추가.
  더불어 `'8개 키'`처럼 **하드코딩된 개수 문구를 배열 길이 기반으로** 바꿈 — 숫자가 문구에
  박혀 있으면 키를 늘려도 눈치채기 어려움

교훈: **기능을 추가하면 그 기능을 검증하는 스크립트도 같이 늙는다.** 특히 "✅ 통과"는
안심의 근거가 아니라 **무엇을 검사했는지 확인해야 할 신호**입니다.

역할 분담도 명확히 해둡니다. `verify-read-api.mjs`의 필드 검사는 **키 존재만** 보므로
`Lunar` 컬럼이 시트에 없어도 통과합니다(`row.Lunar`가 undefined면 `lunarBirthdate: false`로
내려가 키는 존재). **컬럼 실재 보증은 `verify-sheets.mjs`의 몫**입니다.

### `verify-data.mjs`는 행 번호를 알려주지 못한다

시트 행을 객체로 변환하며 인덱스를 버리므로 **건수만 셀 수 있습니다.** 수동 수정이 필요한
행 위치는 별도 일회성 스크립트로 찾았습니다(스크래치패드, 저장소 미포함). 같은 작업이
다시 필요하면 재작성해야 합니다. 스크립트에 하드코딩된 기대 행 수(`359행 기대` 등)도
사본 시절 값이라 실시트에서는 무의미합니다.

### 전환 후 남긴 보류 항목

의도적으로 열어둔 데이터 문제입니다. 둘 다 코드로 판단할 수 없고 담당자 확인이 필요합니다.

- **Students 272행** (Class 빈값): 몇 반인지 확인 불가로 보류. 새 앱의 "확인 필요" 그룹에
  노출되어 추적 가능한 상태(설계 의도대로 동작). 레거시는 이 행을 숨겼으므로 **레거시 266 vs
  새 앱 267의 1명 차이는 정상**이며, 이 행이 정리되면 해소됩니다.
- **Students 205행(오전 2-3) ↔ 209행(오전 2-4)**: 이름+생년월일 동일. 같은 학생의 두 반
  중복 등록 의심이나 담당자 확인 필요로 보류. **레거시도 양쪽 모두 집계**하므로 대조로는
  잡히지 않으며, 중복이 맞다면 두 시스템이 똑같이 1명을 과다 집계 중입니다.

---

## 출석 현황 기간 조회 추가 (2026-08)

`/history`가 올해 일요일 중 **하루**만 볼 수 있어 "최근 한 달 우리 반 평균 출석은?",
"이번 분기에 계속 안 나온 학생은?" 같은 질문에 답할 수 없었습니다. 기존 1주 선택 UI를
그대로 둔 채 여러 주를 묶어 보는 기능을 얹었습니다.

주요 파일: `api/attendance/range/route.ts`, `lib/attendance-range.ts`,
`hooks/useAttendanceRange.ts`, `components/history/{ServiceDateSelector,WeeklyTrendChart,
AttendanceHighlights}.tsx`, `components/common/AlertDialog.tsx`.

### 집계 단위: 연인원 합계가 아니라 "주당 평균 인원"

기간 4주에 반 출석이 총 32회면 `32/8=4.0명`으로 보여줍니다. 합계(연인원)로 하면 숫자가
커져서 **1주 모드와 스케일이 달라지고**, 모드를 오갈 때 "우리 반이 늘었나 줄었나"를 바로
비교할 수 없습니다. 평균은 1주 모드의 정수와 같은 축 위에 있어 전환이 자연스럽습니다.

### `weeks=1`을 특수 케이스로 두지 않음 — 1주 모드는 "기간 1주"일 뿐

화면 아래로 흘려보내는 값을 `attendCounts: Map<id, 출석횟수>` + `weeks: number` **한 쌍으로
통일**했습니다. 1주 모드는 출석자에게 1을 넣고 `weeks=1`을 주면 계산 결과가 기존과 완전히
같아집니다. 덕분에 `GroupAttendanceChart`·`AttendanceListModal`에 분기가 생기지 않고,
**기존 1주 화면에 회귀가 날 경로 자체가 없습니다.**

같은 이유로 `useAttendance`(출석체크 메인의 연타 경합 대책이 들어간 토글 훅)는 손대지 않고
읽기 전용 `useAttendanceRange`를 따로 뒀습니다. 두 훅을 `enabled`로만 스위치하므로
주일에 `/` → `/history` 이동 시 쿼리 캐시(`["attendance", date, session]`) 공유도 유지됩니다.

### 분모는 "실제 예배가 있던 주" — 단 1주 모드는 예외

수련회·명절처럼 예배가 없던 주를 그대로 세면 평균이 내려갑니다. `/api/stats`,
`/api/stats/rates`가 이미 쓰던 규칙(출석 기록이 하나라도 있는 날짜만 카운트)을 따랐습니다.
요일을 따지지 않으므로 특별 예배일도 자연히 포함됩니다.

**1주 모드는 분모를 항상 1로 고정**합니다. 이 규칙을 적용하면 기록이 없는 날에 0으로 나누게
되고, 사용자가 특정 날짜를 콕 집었다면 "0명 / 0%"를 보여주는 쪽이 맞기 때문입니다.

### 기간 조회는 시트 1회 읽기 — 주별 N회 호출 금지

Attendance 시트는 5,000행이 넘고 1회 읽기에 400~700ms입니다. 주별로 `/api/attendance`를
반복 호출하면 그대로 N배가 됩니다. `/api/attendance/range?from=&to=`가 **한 번 읽어 날짜별로
묶어 반환**합니다(`{ dates: { "2026-07-05": [id...] } }`). 세션 필터는 서버가 하지 않고
화면이 roster로 좁힙니다 — 단일 조회 라우트가 이미 명시한 철학과 같습니다.

응답 크기는 1년 범위에서도 100KB대라 문제되지 않습니다. 폴링은 **종료일이 가장 최근
예배일일 때만** 켭니다(지나간 구간은 값이 변하지 않음).

### 조회 범위를 올해로 제한한 이유 — 작년은 "데이터가 없다"

출석부는 1년 단위로 시트를 새로 만들고 과거 연도는 **별도 스프레드시트**에 있습니다
(`docs/yearly-sheet-operation.md`). 앱은 `GOOGLE_SPREADSHEET_ID` 하나만 읽으므로 현재
시트에 작년 데이터가 아예 없습니다. 따라서 기간 선택지는 `sundaysThisYear()` 목록 밖으로
나가지 않게 **인덱스 계산 + 끝에서 clamp**합니다(날짜 산술로 거슬러 올라가면 안 됨).

작년을 고를 수 있게 두면 **"선택은 되는데 결과가 항상 0건"인 UI**가 됩니다. 연도를
넘나드는 조회는 같은 문서 4절(연도→시트 ID 매핑)의 별도 확장 과제입니다.

### 출석률은 화면에 보이는 숫자에서 파생시킨다

평균 109.5명을 110으로 반올림해 표시하면 정확한 평균 출석률(41.48%)과 최대 1%p 어긋납니다.
그래도 **`110 / 264 = 42%`처럼 화면 안에서 산술이 맞아떨어지는 쪽**을 택했습니다. 정확값을
쓰면 "264 중 110인데 41%"가 되어 오히려 오류로 읽힙니다.

### 평균 숫자는 반드시 "무엇의 평균인지" 함께 보여야 한다

`출석 111`만 있으면 그날 하루 출석으로 오해합니다. `SummaryBar`에 `caption` prop을 더해
`4주 평균`을 붙였는데, 처음에 `compact`(스크롤 시 상단 고정 바)에서는 숨겼던 것이 잘못이었습니다.
**스크롤한 상태가 오히려 오독 위험이 큽니다** — 날짜 컨트롤과 구간 배지가 전부 화면 밖이라
남는 단서가 숫자뿐이기 때문입니다. compact에서는 세로 여백만 줄이고 문구는 유지합니다.

### recharts v3 함정 ①: 차트 레벨 `onClick`은 발화하지 않는다

면적 차트에서 `<ComposedChart onClick={...}>`으로 클릭한 주를 받으려 했으나, **툴팁이 떠 있는
상태에서 실제 클릭을 해도 핸들러가 호출되지 않았습니다.** 대신 **투명 막대를 클릭 타깃으로
깔고 `Bar`의 `onClick`을 씁니다**(막대 onClick은 다른 차트에서 이미 검증됨).

- 막대 높이는 y축 도메인 최댓값으로 채워 차트 어디를 눌러도 잡히게 함
- `barCategoryGap={0}` 필수 — 기본 간격을 두면 주와 주 사이에 **눌러도 반응 없는 죽은 구간**이 생김(측정 시 29px)
- 툴팁 목록에서 빼려면 `tooltipType="none"` (커스텀 tooltip content로 필터링하면 타입이 깨짐)

### recharts v3 함정 ②: `ui/chart.tsx`의 축 눈금 셀렉터가 v2 기준이라 무효였다

```
v2: <g class="recharts-cartesian-axis-tick"><text>…       ← chart.tsx가 기대한 구조
v3: <text class="recharts-text recharts-cartesian-axis-tick-value">
```

`[&_.recharts-cartesian-axis-tick_text]:fill-muted-foreground`가 **매칭조차 되지 않아**, 모든
차트의 축 글자가 팔레트가 아닌 recharts 기본 회색 `#666`으로 그려지고 있었습니다.
`.recharts-cartesian-axis-tick-value`로 고치면서 색도 `ink/70`으로 바꿨습니다 —
`muted-foreground`(#737373)는 shadcn 기본 무채색인 데다 paper-deep 위에서 4.29:1로 **AA 미달**입니다.

⚠️ 같은 파일에 `[stroke='#ccc']`처럼 **하드코딩된 값에 의존하는 v2 시절 셀렉터가 더 있습니다.**
현재 차트들이 그리드를 쓰지 않아 드러나지 않을 뿐이니, 새 차트를 추가할 때 확인하세요.
개별 차트에서 같은 속성을 다시 지정하면 **특정도가 같아 승자가 스타일시트 순서로 결정**되므로,
공통 규칙에 맡기고 로컬 오버라이드는 두지 않습니다.

### 이 팔레트의 강조색은 작은 글씨에 쓸 수 없다 (측정치)

"흐릿하니 색을 넣어 강조하자"가 **거의 항상 역효과**입니다. 배경별 실측 대비율:

| 배경 | stamp | teal | gold | celebrate | ink 계열 |
|---|---|---|---|---|---|
| `paper-deep` (밝음) | 2.84 | 3.97 | 3.91 | 2.89 | ink/75 **5.57** · ink **11.58** |
| `ink` (어두움) | 4.08 | 2.92 | 2.97 | 4.01 | paper/55 4.88 · paper **12.41** |

12px 텍스트 기준 AA는 4.5:1인데 **강조색은 어느 배경에서도 통과하지 못합니다.**
그래서 규칙을 이렇게 정했습니다 — **글자는 대비가 확보되는 ink/paper 계열로 두고, 색은
배경(틴트 배지)이 담당한다.** 틴트는 휘도차가 1.1~1.3:1로 작지만 **색상(hue)이 달라 실제로는
잘 구분됩니다** — 같은 화면의 개근/결석 칩이 1.14:1로 이미 잘 읽히는 것이 근거입니다.

강약은 색이 아니라 **불투명도와 굵기**로 줍니다. 예: 추이 차트에서 x축 날짜는 `ink/70`(4.85:1),
출석 숫자는 `ink` + semibold(11.56:1) — 축과 데이터가 한눈에 다른 정보로 읽힙니다.

### 차트 표식은 눈금마다 꾸미지 말고 범례로 한 번만 설명한다

"날짜 앞에 달력 아이콘"을 검토했으나 52주 조회에서 축이 지저분해집니다. 대신 **우측 하단
범례**에 세 표식(`● 주간 출석 인원` / `┄ 평균 N명` / `📅 예배일`)의 이름을 한 번만 답니다.

같은 이유로 평균선 라벨도 `ReferenceLine`의 자체 label을 쓰지 않습니다 — `insideTopRight`가
마지막 주 데이터와 겹칩니다.

### 추이 차트의 y축은 0에서 시작하지 않는다 (의도적)

전체 인원(수백 명)까지 축을 늘리면 주간 변화(보통 10명 안팎)가 몇 %의 높이로 뭉개져 **'추이'가
전혀 보이지 않습니다.** 데이터 범위로 좁히되, 기준을 잃지 않도록 ① 평균 점선 ② 점 위 숫자
③ 헤더의 `명단 N명 기준`을 함께 둡니다. 전체 대비 비율은 바로 위 요약바와 아래 반별 차트가
이미 보여주므로 이 차트는 변화만 담당합니다.

### 입력 유효성: 되돌리기는 setter를 호출하지 않는 것으로 충분하다

`직접 지정`에서 시작일 > 종료일이면 경고하고 선택을 되돌립니다. select가 props로 제어되므로
**setter를 호출하지 않으면 React가 이전 값으로 다시 렌더**해 자동 복귀합니다. 별도 롤백 코드가
필요 없습니다. 같은 날짜는 1주 조회로 유효하므로 막지 않습니다.

경고창은 네이티브 `alert()` 대신 `components/common/AlertDialog.tsx`(프로젝트 Dialog 기반)를
씁니다 — `alert()`은 브라우저가 `localhost:3000 says` 같은 문구를 덧붙이고 앱 팔레트와 겉돕니다.
두 날짜를 **라벨-값으로 세로 정렬**해 무엇이 뒤집혔는지 한눈에 보이게 했습니다.

`lib/attendance-range.ts`의 `resolveRange`에는 스왑 정규화를 **최후 방어선으로 남겨뒀습니다.**
화면이 막고 있어 도달할 일은 없지만 순수 함수라 어떤 입력이든 받을 수 있고, `from > to`가
API로 나가 400을 받는 상태만은 막아야 하기 때문입니다.
